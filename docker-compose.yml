services:
  postgres:
    image: postgres:16-alpine
    container_name: risk-console-postgres
    environment:
      POSTGRES_DB: risk_console
      POSTGRES_USER: risk
      POSTGRES_PASSWORD: risk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
